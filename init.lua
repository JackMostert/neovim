local config = {
  colorscheme = "vscode",
  lsp = {
    diagnostics = {
      virtual_text = false,
      underline = true,
    },
    formatting = {
      format_on_save = false,
    },
  },
}

return config
